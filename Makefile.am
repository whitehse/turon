## top directory

#Build in these directories:
SUBDIRS= $(GENERIC_LIBRARY_NAME) examples

#Distribute these directories:
DIST_SUBDIRS = $(GENERIC_LIBRARY_NAME) examples

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = turon-1.0.pc

EXTRA_DIST=m4
